# PRD：通用游戏报错解决专家（Game Doctor）

| 字段 | 内容 |
|---|---|
| 文档版本 | v2.1（v2.0 基础上新增通用自动修复能力） |
| 创建日期 | 2026-09-04 |
| 产品负责人 | （待填写） |
| 状态 | Draft |
| 优先级 | P1 |

---

## 1. 产品定位

### 1.1 一句话描述

一款 CLI 通用游戏故障诊断与修复工具：自动识别游戏技术栈（引擎 + 运行时 + 平台），解析日志与环境信息，结合本地知识库与多源联网检索定位根因，并**以"通用修复原语 + 分级授权 + 可回滚"的方式自动执行修复**，而非只给建议。

### 1.2 定位转变

| | v1.0 | v2.0 | v2.1（本版） |
|---|---|---|---|
| 范围 | 单一游戏 MOD 助手 | 通用报错诊断专家 | 通用诊断 + **自动修复**专家 |
| 知识来源 | 硬编码规则 | 元数据指纹 + 动态检索 | 同左 |
| 修复执行 | 只给建议 | 只给建议 | **分级自动修复**（白名单直修 / 高危确认 / 全程可回滚） |
| 核心理念 | "我知道这个游戏怎么修" | "我能快速理解陌生游戏的技术上下文" | "我能理解上下文，并用安全可控的方式把它修好" |

**通用性的本质是元认知能力，而非知识覆盖面。自动修复的通用性来自"修复原语 + 技术栈参数化"，而非为每个游戏写死脚本。**

### 1.3 边界（明确不做）

- ❌ 不做 MOD 下载/安装管理、不做游戏启动器
- ❌ 不做**无备份**的破坏性自动修复；L3 高危操作（注册表/系统 DLL/BIOS）永不自动执行，只给指引
- ❌ 不支持私服 / 破解版 / 未发布测试版（识别后明确告知支持边界）
- ❌ v1 仅 CLI，不做 GUI

---

## 2. 技术栈总览（核心选型）

> 开发直接依据。原则："代码先做 80% 的结构化工作，LLM 只做最后 20% 推理"；自动修复遵循"先备份、可验证、可回滚"。

| 层 | 选型 | 说明 / 备选 |
|---|---|---|
| 语言 | Python 3.10+ | 跨 Win / macOS / Linux |
| CLI 框架 | Typer + Rich | Rich 负责报告与进度渲染 |
| LLM 接入 | httpx → deepseek / Qwen | 多模型路由 + 交叉审查；离线用 Ollama |
| 联网搜索 | Tavily（主）/ SerpAPI / DuckDuckGo（兜底） | 三级降级 |
| 结构化知识源 | PCGamingWiki / ProtonDB / WineHQ API | 权威源，优先级高于泛搜索 |
| 本地知识库 | SQLite + 可选 chromadb | 错误签名匹配 + 向量检索 |
| 指纹识别 | 引擎签名库 + psutil + 系统命令 | 见 §4.1 |
| **修复执行** | 修复原语库 + 事务化执行器（备份/回滚） | 见 4.5；基于 shutil / subprocess / 平台 CLI |
| **平台接口** | Steam（steamcmd / 校验完整性）/ Epic / GOG CLI | 修复原语的平台适配 |
| 配置管理 | TOML（tomllib + tomli-w），Keyring 存密钥 | ~/.gamedoctor/config.toml |
| 打包分发 | pipx / PyInstaller | 单文件可选 |

---

## 3. 总体架构

```
gamedoctor diagnose [--fix]
        │
        ▼
┌─────────────────────────────────────────────┐
│ ① 指纹识别层：游戏 → 技术栈组合              │
├─────────────────────────────────────────────┤
│ ② 信息采集层：日志解析 / 文件扫描 / 环境指纹 │
├─────────────────────────────────────────────┤
│ ③ 知识检索层：本地 RAG → 多源联网检索        │
├─────────────────────────────────────────────┤
│ ④ LLM 推理层：分类 → 方案 → 交叉自检         │
├─────────────────────────────────────────────┤
│ ⑤ 自动修复层：原语编排 → 分级授权 →          │
│    备份 → 执行 → 验证 → 失败回滚             │
├─────────────────────────────────────────────┤
│ ⑥ 输出层：置信度分级 + 修复结果 + 结构化报告 │
└─────────────────────────────────────────────┘
```

---

## 4. 核心功能模块

### 4.1 F1：「游戏-引擎-运行时」三层指纹识别（P0）

不预设游戏列表，把"这是什么游戏"转化为"这是什么技术栈组合"，为诊断与修复同时提供参数。

| 子层 | 内容 | 实现 |
|---|---|---|
| 引擎特征库 | UnityPlayer.dll、UnrealEd.exe、libgodot.so、Ren'Py 等签名/命名规则 | 文件哈希 + 命名模式匹配，可增量维护 |
| 运行时探测 | DX 版本、VC++ Redistributable、.NET、Java、Wine/Proton、显卡驱动与 API | 系统命令 + 注册表 + psutil |
| 平台适配层 | Steam / Epic / GOG / 独立版 / 模拟器 | 区分日志位置与修复手段（Steam verify integrity vs Epic repair） |

### 4.2 F2：通用错误分类学（P0）

| 通用错误类别 | 典型表现 | 共性排查方向 |
|---|---|---|
| 运行时崩溃 | Access Violation, Segfault, EXC_BAD_ACCESS | 内存 / 驱动 / 引擎 Bug / Mod 冲突 |
| 启动失败 | Missing DLL, Entry Point Not Found, 0xc000007b | 运行时缺失 / 架构不匹配（x86/x64） |
| 渲染异常 | Black Screen, TDR, Device Lost, Shader Error | GPU 驱动 / DX-VK 转换 / 显存不足 |
| 资源加载 | File Not Found, Corrupt Asset, Checksum Mismatch | 文件完整性 / Mod 覆盖 / 路径编码 |
| 网络/联机 | Timeout, NAT Type, Auth Failed, EAC/BattlEye | 防火墙 / 加速器 / 反作弊 / 服务器状态 |
| 性能问题 | Stutter, Low FPS, Memory Leak | 设置过高 / 后台占用 / 引擎优化缺陷 |
| 存档/配置 | Save Corrupt, Settings Reset, Cloud Sync Conflict | 权限 / 云同步 / Mod 改存档结构 |
| 兼容性问题 | Wine/Proton 报错, Rosetta, ARM 转译 | 翻译层 / 指令集 / 文件系统大小写 |

### 4.3 F3：动态知识获取（P0/P1）

| 策略 | 说明 |
|---|---|
| 结构化数据源优先 | PCGamingWiki / WineHQ / ProtonDB API，优先于泛搜索 |
| 多源搜索模板化 | 按引擎/平台预制 Query 模板（UE→reddit/steamcommunity，Proton→protondb，Unity→github issues） |
| 本地知识沉淀 | 成功修复后提取 (游戏指纹, 错误特征, 修复动作序列) 三元组存入本地 RAG |
| 限流 | 每次诊断最多 5 次搜索，每次取 Top 5 |

### 4.4 F4：交互式诊断流程（P1）

- **首轮必问三要素**：① 游戏名 + 平台 ② 完整错误信息 / 截图 ③ 问题发生时机（更新后 / 装 Mod 后 / 一直有）
- **日志自动定位器**：内置主流引擎/平台日志路径映射表（如 UE `%LOCALAPPDATA%/{Game}/Saved/Logs`、Unity `output_log.txt`）
- **分步验证协议**：每个修复建议附可证伪检查点，禁止"试试更新驱动"这类无法验证的模糊建议
- **信息不足不硬猜**：明确列出缺失项，进入多轮追问

### 4.5 F5：通用自动修复执行引擎（P0/P1，本版核心）

**通用化关键：修复动作以「修复原语」组织，由 §4.1 的技术栈指纹填参，而非为每个游戏写死脚本。**

#### 4.5.1 修复原语库（按操作类型，非按游戏）

| 原语类别 | 典型动作 | 技术栈参数化示例 |
|---|---|---|
| 文件级 | 移除/隔离冲突 Mod、清理缓存、修复缺失资源 | Mod 目录路径由指纹识别给出 |
| 配置级 | 改配置文件、调启动参数、降渲染 API（-dx11） | 配置文件路径/格式由引擎决定 |
| 平台级 | Steam 校验完整性、Epic repair、清云存档冲突 | 由平台适配层选择对应命令 |
| 运行时级 | 安装/修复 VC++、.NET、DirectX 运行库 | 缺失组件由运行时探测给出 |
| 系统级 | 驱动建议、防火墙规则 | 多数仅引导，不自动执行 |

#### 4.5.2 动作分级授权（安全核心）

| 级别 | 授权方式 | 示例 |
|---|---|---|
| L0 只读检查 | 无需确认 | 校验文件、探测环境 |
| L1 安全自动 | 白名单，备份后直接执行 | 改配置、禁/移 Mod、清缓存、平台校验完整性 |
| L2 需确认 | 用户逐项确认后执行 | 安装/更新运行时、存档修复、批量文件操作 |
| L3 禁止自动 | 永不执行，仅给指引 | 注册表、系统 DLL、BIOS、卸载系统组件 |

#### 4.5.3 事务化执行闭环（备份 → 执行 → 验证 → 回滚）

```
--fix 触发
  ├─ 预检：生成修复计划 + 影响清单，向用户展示
  ├─ 备份：对将被改动的文件/配置做快照（~/.gamedoctor/backups/<诊断ID>/）
  ├─ 执行：按优先级逐项应用原语（L1 直跑，L2 逐项确认）
  ├─ 验证：每步后跑对应检查点（复用分步验证协议）
  └─ 回滚：任一步验证失败 → 自动还原备份，并报告失败点
```

- **Dry-run 模式**：`gamedoctor diagnose --fix --dry-run` 只输出将执行的动作与影响，不实际改动
- **单步模式**：`--step` 每步暂停等待确认，适合谨慎用户
- **一键回滚**：`gamedoctor rollback --id <诊断ID>` 手动还原最近一次修复

### 4.6 F6：LLM 推理管线（P0）

| 步骤 | 模型策略 | 说明 |
|---|---|---|
| 错误分类 | 轻量模型 / 正则先行 | 快、便宜 |
| 日志摘要 | 轻量模型 | 不需强推理 |
| 方案生成 | 强模型 | 输出结构化"修复动作序列"供执行引擎消费 |
| 方案自检 | 与生成不同的模型 | 交叉审查 |
| 离线场景 | Ollama 本地模型 | 隐私优先兜底 |

> 自动修复要求方案生成输出**机器可执行的动作序列**（原语 + 参数 + 验证检查点），而非纯自然语言建议。

### 4.7 F7：安全边界与免责（P0，随自动修复升级）

| 机制 | 说明 |
|---|---|
| 分级授权 | 见 §4.5.2；L3 永不自动执行 |
| 强制备份 | 任何写操作前必备份，且备份可一键回滚 |
| 高危操作拦截 | 注册表/系统 DLL/BIOS → 强制二次确认 + 仅给指引 |
| 置信度分级输出 | 区分「官方文档确认」/「社区高赞」/「AI 推理推测」 |
| 不支持声明 | 识别私服 / 破解版 / 测试版时明确告知边界 |
| 隐私 | 日志仅发送给所选 LLM，不上传第三方；支持纯本地模式 |

### 4.8 F8：反馈闭环与知识沉淀（P2）

```
gamedoctor feedback --id <诊断ID> --result fixed/partial/failed
```
- fixed → 知识条目有效计数 +1；failed → -1 并标记复查
- **成功自动修复的动作序列**沉淀为可复用修复模板，同技术栈同类问题下次可直接复用

---

## 5. 输出报告格式

```
═══════════════════════════════════════
  🔍 诊断报告  #20260904-001
  技术栈: UE5.2 | DX12 | Steam | Win11 | RTX 4060
═══════════════════════════════════════
### 🎯 根因判断
[渲染异常] 置信度: 高 | 来源: 官方文档确认
DX12 下 Shader 编译失败，与驱动 551.86 已知问题匹配。

### 🛠️ 修复方案（按优先级）
1. 启动项加 -dx11 降级渲染        [L1 可自动]
   → 验证: 确认正常进入游戏主菜单
2. 回滚显卡驱动至 546.33          [L3 仅指引]
   → 验证: 启动游戏，通过开场 Shader 编译

### ⚙️ 自动修复执行结果（--fix）
- 备份: ~/.gamedoctor/backups/20260904-001/ ✅
- [1/2] 修改启动配置加 -dx11 ...... ✅ 验证通过
- [2/2] 驱动回滚 ................... ⏭️ 需手动（L3）
- 结论: 1 项已自动修复，1 项需手动

### ⚠️ 风险提示 / 🔗 参考资料（标注来源分级）
═══════════════════════════════════════
```

---

## 6. 落地路线图（按优先级）

| 优先级 | 内容 |
|---|---|
| **P0** | 指纹识别 + 通用错误分类 + LLM 诊断管线 + **L1 白名单自动修复（含备份/回滚）** + 高危拦截 |
| **P1** | 多源搜索模板 + 日志路径映射 + 分步验证协议 + **L2 确认式修复 + Dry-run/单步模式** |
| **P2** | 反馈闭环 + 修复模板沉淀 + 本地 RAG |
| **P3** | 多模型交叉审查 + 离线兜底模式 |

---

## 7. 成功指标

| 指标 | 目标值 | 采集方式 |
|---|---|---|
| 技术栈识别准确率 | ≥ 90%（已收录引擎） | 指纹识别回归测试 |
| 用户反馈"已修复"比例 | ≥ 70%（v1） | feedback 命令 |
| **自动修复成功率**（L1 场景） | ≥ 80% | 执行 + 验证日志 |
| **回滚成功率** | 100%（凡备份必可还原） | 回滚测试 |
| 平均诊断耗时 | ≤ 3 分钟 | 计时器 |
| 高危操作误执行数 | 0（拦截生效） | 拦截日志 |

---

## 8. 风险与对策

| 风险 | 对策 |
|---|---|
| 自动修复误伤正常文件 | 强制备份 + 白名单限定 + 验证失败自动回滚 |
| 技术栈碎片化导致识别失败 | Generic 兜底 + 用户手动补充 + 指纹库增量更新 |
| LLM 幻觉给出错误修复动作 | 结构化输入 + 交叉审查 + 动作须经执行引擎校验后才落地 |
| 长尾游戏无知识库 | 多源检索模板 + 修复模板自动沉淀 |
| 高危修复误操作 | 分级授权 + L3 永不自动 + 二次确认 |
| 搜索质量不稳定 | 结构化数据源优先 + 三级搜索引擎降级 |
| API Key 泄露 | 系统 Keyring 存储，不明文打印 |

---

## 附录：项目结构

```
game-doctor/
├── pyproject.toml
├── prd.md                  ← 本文档
├── src/gamedoctor/
│   ├── cli.py              # Typer CLI 入口（diagnose/--fix/rollback）
│   ├── config.py           # TOML 配置 + Keyring
│   ├── fingerprint/        # 引擎签名库 / 运行时探测 / 平台适配
│   ├── analyzer/           # 日志解析 / 文件扫描 / 环境指纹
│   ├── knowledge/          # SQLite RAG + 向量检索（可选）
│   ├── searcher/           # 搜索模板引擎 + 数据源适配器
│   ├── llm/                # API 封装 + 模型路由
│   ├── solver/             # 方案生成（输出动作序列）/ 交叉自检
│   └── fixer/              # 自动修复核心
│       ├── primitives/     #   修复原语库（文件/配置/平台/运行时）
│       ├── policy/         #   分级授权（L0-L3 白名单/拦截）
│       ├── executor/       #   事务化执行器（备份/执行/验证/回滚）
│       └── backup/         #   备份与回滚管理
└── tests/
```
