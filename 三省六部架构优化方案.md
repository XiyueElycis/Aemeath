# 三省六部架构优化方案（基于代码实读 + 运行时实测）

> 编制日期：2026-09-16
> 依据：通读 `src/gamedoctor/` 治理层、编排层、12 个领域智能体、指纹层、知识库层全部源码，
> 并在本机搭建仿真游戏目录（Unity 特征文件 + Mods + saves + EasyAntiCheat + 含真实报错的日志）
> 实跑验证。**文中每一条缺陷都附实测证据，非静态推测。**

---

## 一、结论速览

### 问题 1：三省六部架构优化方案

制度层（任务票状态机 / 权限矩阵 / 规划官 / 审议官）**设计质量高，实测工作正常**，
不需要推倒重来。真正的问题在**执行层与采集层**：

| 层面 | 状态 | 说明 |
|---|---|---|
| 状态机 + 流转日志 | ✅ 良好 | 单向递进、终态锁定、封驳回路，实测合法 |
| 权限矩阵 `can_dispatch` | ✅ 良好 | 5 个任务全部正确放行，无越级 |
| 审议官（纯规则） | ✅ 良好 | 实测 `approved=True`，**0 次 LLM**，DAG 环检测/白名单/模式红线齐备 |
| 规划官模板路径 | ✅ 良好 | 4 类意图正则分拣实测全部命中，**0 次 LLM** |
| **指纹层 ↔ 治理链路** | ❌ **完全断开** | 能采到的信息没送进智能体 |
| **`utils/system_info.py`** | ❌ **残缺且含死代码** | 有函数从未跑通（`NameError`） |
| **兼容性判定** | ❌ **恒误报** | 本机 RTX 4070 / Win11 被判为不合格 |
| **知识库 33 篇文档** | ❌ **零命中** | 灌库缺失 + 导入路径错位 |

### 问题 2：无大模型依赖的智能体，接入游戏根目录后能否自动获取相关信息？

**部分能，但存在系统性断层。** 实测结论：

- ✅ **真能自动采到**：`log_analyzer`（扫盘提日志报错）、`perf_security`（反作弊文件特征 + 进程枚举）、
  `game_content`（Mod 扫描 / 冲突检测 / 存档定位与备份）、`launch_optimizer`（后台进程 Top5）
- ⚠️ **只采到系统级、采不到游戏级**：`compatibility`、`network`
- ❌ **完全不采集，纯占位返回**：`audio`、`social`、`community`
- ❌ **无视传入的 `game_dir`**：`installation`

**根因是一条：指纹层（`fingerprint/`）已具备完整采集能力，但只被旧的 `pipeline.py` 调用，
三省六部治理链路一次都没调用过它。**

实测对照（同一台机器、同一个游戏目录）：

```
指纹层能取到：gpu = NVIDIA GeForce RTX 4070 Laptop GPU
             driver = 32.0.16.1656      dx = 12 Ultimate
             dotnet = 4.8               vc = 10 个组件
             dx9_legacy = True          engine = unity (confidence 0.8)

智能体实际收到：gpu = "Unknown GPU"     memory = 0
               os = "Windows 10 Home"  version = "6.3"
               platform = "standalone"  app_id = None
               version = None           dlcs = []   size_mb = 0
```

信息已经在手上，只是没接上线。

---

## 二、现有架构实况：两套并行且互不相通的链路

这是最需要优先解决的结构性问题。项目里同时存在两条完整编排链，共享智能体但**不共享上下文**：

```
链路 A（旧）：pipeline.py :: DiagnosisPipeline.run()
  指纹识别 → 信息采集 → 知识检索 → LLM 推理 → 自动修复 → 输出
  ✅ 接入了 fingerprint 三件套（pipeline.py:155-157）
  ❌ 知识库检索恒返回空（见 §4.7）
  ❌ LLM 失败时 plan.actions=0，整链退化为"只有风险列表、零修复动作"
     实测：risks = ['方案生成失败：LLM 返回 HTTP 401']，actions = 0

链路 B（新）：governance/ :: GovernanceCoordinator.run()
  接待官 → 规划官 → 审议官 → 调度官 → 12 智能体 → 验收 → 回奏
  ✅ 制度化、可审计、状态机严格
  ❌ 构造 GameContext 时只填 2 个字段（coordinator.py:239-241）：
        context = GameContext(game_name=game_name)
        if game_dir:
            context.install_path = Path(game_dir)
     → 指纹层完全未接入，下游智能体拿不到引擎/运行时/平台信息
```

`GameContext`（`models.py:330-342`）定义了 10 个字段，治理链路只填了 2 个，
其余 8 个（`install_date` / `last_played` / `playtime_hours` / `platform` /
`app_id` / `version` / `size_mb` / `dlcs`）永远是默认值。

**另注：文档与代码的数字不一致。** `governance/__init__.py:29` 与 `README.md` 均称
"六部 = 15 个领域智能体"，实测 `_AGENT_REGISTRY`（`agent_factory.py:197-210`）与
`DEFAULT_AGENT_CONFIGS` 均为 **12 个**——`update/save/dlc` 已合并进 `game_content`、
`performance/security` 已合并进 `perf_security`，文档未同步。

---

## 三、无模型智能体自动采集能力实测矩阵

仿真目录：`%TEMP%\gd_probe\TestGame`，含 `UnityPlayer.dll`、`TestGame.exe`、
`Mods\BetterWater.pak`、`Mods\BetterWater.esp`、`saves\slot1.dat`、
`EasyAntiCheat\EasyAntiCheat_EOS.exe`、`GameDir\output_log.txt`（内含
`NullReferenceException` 与 `missing dll d3dx9_43.dll`）。

| 智能体 | LLM 依赖 | 自动采集 | 实测结果 |
|---|---|---|---|
| `log_analyzer` | **可选**（有回退） | ✅ **有效** | 扫到 1 个日志、提取 2 条报错；**无密钥时自动回退正则报告**，实测 notes 含"大模型不可用，已回退为关键词提取报告" |
| `perf_security` | 无 | ✅ **部分有效** | 安全侧：**真实识别出 EasyAntiCheat**，冲突提示准确；进程枚举走 psutil 真实。性能侧：因 `gpu memory=0` 导致显存瓶颈判定失效 |
| `game_content` | 无 | ✅ **部分有效** | `dlc`：真实扫到 2 个 Mod 并报出同名冲突；`saves`：真实定位 + **完成备份落盘**（`~/.gamedoctor/saves_backup/TestGame/20260916-093951`）；`version`：全 None/占位 |
| `launch_optimizer` | 无 | ⚠️ **半有效** | 后台进程 Top5 真实采到（Trae CN.exe 0.7GB / steamwebhelper.exe 0.5GB …）；但启动参数靠**游戏名字符串**猜引擎，指纹层已识别出 unity 却没用上 |
| `compatibility` | 无 | ⚠️ **只采系统级且失真** | GPU=Unknown、OS 误判为 Win10；2 条 critical **全为误报**；overall_score=60 |
| `installation` | 无 | ❌ **无视 game_dir** | 传入 `...\TestGame`，算出 `C:\Users\Kirano星岚\Games\TestGame`；目录内明明有 `TestGame.exe`，却报"未找到主程序" |
| `network_expert` | 无 | ❌ **仅硬编码** | `_detect_nat()` 恒 `unknown`（TODO: STUN）；端口表恒返回同一组，与游戏无关；`latency_ms=None` |
| `audio_expert` | 无 | ❌ **纯占位** | 设备恒 `["默认输出设备"]`（TODO: WASAPI）；格式问题恒返回同一句固定文案 |
| `social_agent` | 无 | ❌ **纯占位** | 好友恒 `[]`；语音恒 `["游戏内语音","Discord"]`；账号恒 `["steam"]`；隐私设置全硬编码 |
| `community_agent` | 无 | ❌ **纯占位** | 三个方法全 TODO，恒返回 `f"{game_name} 新手入门"` 类拼接串 |
| `script_editor` | **必需**（edit/create） | — | 读/列/删不需 LLM；生成内容需 LLM |
| `web_search` | 外部搜索 API | — | 依赖 Tavily 等，非本地采集 |

**零模型可用集合**：`log_analyzer` + `perf_security` + `game_content(dlc/saves)` +
`launch_optimizer(后台进程)`，共 4 个智能体的部分能力。这是当前唯一真实有效的自动采集面。

---

## 四、缺陷清单（分级，附实测证据与代码位置）

### P0 — 阻断核心功能，必须优先修

#### 4.1 指纹层与治理链路断开
- **位置**：`coordinator.py:239-241` 只填 2 字段；`fingerprint/` 三件套仅 `pipeline.py:155-157` 调用
- **证据**：实测 `GameContext` 中 `platform='standalone' app_id=None version=None dlcs=[] size_mb=0`，
  而 `probe_runtime()` 同时能返回 RTX 4070 / DX12 Ultimate / .NET 4.8 / 10 个 VC++ 组件
- **影响**：所有下游智能体在信息真空里工作，只能靠硬编码或游戏名猜

#### 4.2 `utils/system_info.py` 是从未跑通的残缺采集层
- **`get_system_requirements_check()`（:323-351）**：模块级函数体内用了 `self._is_cpu_compatible(...)`
  → 实测直接 `NameError: name 'self' is not defined`。**该函数自写下起从未成功执行过。**
- **`get_gpu_info()`（:121）**：`gpu.Description.lower().contains("opengl")` —— Python `str` 无 `.contains()`
  方法，是 Java/C# 语法误用。只要装了 `wmi` 且走到这行必抛 `AttributeError`，
  被外层 `except Exception` 吞掉 → 同样退化成 Unknown GPU。**双重故障互相掩盖。**
- **`get_gpu_info()` 整体**：`wmi` 未安装 → 实测恒返回 `Unknown GPU, memory=0`；
  而 `fingerprint/runtime.py:209` 用 PowerShell CIM 拿到了真实型号与驱动版本
- **`get_os_info()`（:189-223）**：实测返回 `name='Windows 10 Home' version='6.3'`
  （注册表 `ProductName`/`CurrentVersion` 是遗留未更新值），真实系统是 Win11 24H2 build 26100；
  `fingerprint` 用 `platform.release()` 正确得到 `Windows 11`
- **`get_directx_info()`（:226-239）**：`Win32_DirectX` 不是真实 WMI 类 → 恒 `Unknown`；
  而 `fingerprint/runtime.py:97` 通过 System32 特征 DLL 正确判定为 `12 Ultimate`

#### 4.3 兼容性判定恒误报
- **位置**：`compatibility_agent.py`
- **证据链**（本机 RTX 4070 / 16GB / Win11 24H2 被判 60 分，2 条 critical）：
  1. `_check_hardware_requirements`（:182-187）需求**硬编码** `{'cpu':'i5-7500','gpu':'GTX 970','ram':8192,'storage':50000}`，不读游戏真实需求
  2. `_is_cpu_compatible`（:305-308）直接 `return True` —— **永真，CPU 检查形同虚设**
  3. 调用 `_is_gpu_compatible(gpu, gpu_memory, requirements["gpu"], requirements["storage"])`（:202）
     —— **第 4 参传的是 `storage`（磁盘 50000MB），语义错配成显存需求**
  4. 因 §4.2 导致 `gpu memory=0`，`0 < 50000` → **恒判"GPU 可能不满足要求"**
  5. `_check_os_compatibility`（:161-164）：`_version_to_float('6.3')` → `6.3 < 10`
     → **恒报"要求 Win10 或更高，当前系统 Windows 10 Home"**，实际是 Win11
- **实测**：`_is_gpu_compatible('RTX 4070', 8GB, 'GTX 970', 50000)` → `True`；
  但走真实数据路径（memory=0）→ `False`。**判定结果完全由采集故障决定，与硬件无关。**
- **附带**：`known_issues_db`（:50-61）是硬编码示例，注释自陈"应该从数据库加载"；
  `_is_high_requirement_game`（:317-320）靠游戏名里有无 "aaa"/"rpg" 判断

#### 4.4 知识库 33 篇文档零命中
- **资产**：`rag/minecraft/`（22 篇）+ `rag/Grand_Theft_Auto_V/`（11 篇）高质量排障文档
- **SQLite 路径**：实测 `knowledge.db` 为空库，`lookup('missing dll')` → `[]`；
  `search_knowledge()` 对 3 个真实签名全部 0 命中。
  **全项目无任何调用 `store.upsert()` 的灌库入口**——库永远是空的
- **向量路径**：`VectorRetriever._check_availability()` → 实测 `False`
  - `retriever.py:55` 写 `from ...rag import rag_data`，从 `gamedoctor.knowledge` 上溯三级
    解析为 **`gamedoctor.rag`（不存在）**；真实模块在**项目顶层 `rag/`**，路径错位
  - 顶层 `rag.rag_data` 依赖 `langchain_core`，实测 **未安装**
  - `config.py:48-49` 默认 `use_vector=False`、`rag_data_dir=""`，未指向真实 rag 目录
- **连带影响**：`pipeline.py` 的"③知识检索"层永远空，`LLMSolver` 成为唯一出方案途径；
  实测 LLM 401 时 `plan.actions=0` → **整条诊断链退化为只有风险列表、零修复动作**

#### 4.5 `installation` 智能体无视传入的游戏目录
- **位置**：`installation_agent.py`
- **证据**：
  - `_optimize_install_path`（:125-141）只查硬编码的 Steam/Epic/standalone 默认路径表
    （:36-57），全不命中就落到 `Path.home()/"Games"` —— **完全不读 `data["game_dir"]`**。
    实测传入 `...\gd_probe\TestGame`，输出 `C:\Users\Kirano星岚\Games\TestGame`
  - `_find_main_executable`（:341-350）只找 4 个固定名 `game.exe/start.exe/main.exe/launcher.exe`
    → 目录内真实存在 `TestGame.exe`，却报 **"未找到主程序"**，`verification.success=False`
  - `_check_steam_game` / `_check_epic_game`（:372-380）恒 `return True`
    → **任何游戏都被判定"在 Steam/Epic 可用"**，`install_source` 因此恒为 `steam`
  - `_estimate_game_size`（:228-238）按游戏名关键词分类，"TestGame" 落 Indie → 恒 5GB
  - `_check_integrity`（:363-370）自陈"简化实现，实际应该计算文件哈希"，`missing_files` 恒 `[]`

### P1 — 严重削弱价值

#### 4.6 四个纯占位智能体产出虚假数据
- `audio_expert.py:74-80`：设备恒 `["默认输出设备"]`、格式问题恒同一句文案（TODO: WASAPI）
- `social_agent.py:67-83`：好友恒 `[]`、语音恒 `["游戏内语音","Discord"]`、
  账号恒 `["steam"]`、隐私设置全硬编码（TODO: 平台好友 API / 本地配置）
- `community_agent.py:69-79`：三个方法全 TODO，恒返回游戏名拼接串
- `network_expert_agent.py:94-105`：`_detect_nat()` 恒 `unknown`（TODO: STUN）、
  `_recommend_servers()` 恒 `["自动选择（最低延迟）"]`、端口表与游戏无关
- **危害**：`success=True` + 结构化数据 → 审议官与验收无法区分"真采到"和"占位"，
  占位内容会被回奏当作真实结论呈现给用户

#### 4.7 `game_content` 版本子项与兼容性判定为占位
- `_fetch_changelog`（:200-202）恒返回 `f"{game_name} 更新到 {latest_version or '最新'}（占位）"`
- `_assess_risk`（:194-198）仅看版本号后缀有无 `beta/rc`
- `_check_dlc_compatibility`（:345-354）硬编码 `if version.startswith("2.")` → 无实际意义
- `_version_sync_status`（:356-359）无论有无 Mod **都 `return "synced"`**
- 实测 `op=version`：`current_version=None latest_version=None changelog=['TestGame 更新到 最新（占位）']`
  → 却给出 `recommendations=['当前已是最新版本']`，**误导性结论**

#### 4.8 `launch_optimizer` 用游戏名猜引擎，弃用真实指纹
- `_build_launch_args`（:74-84）：`if any(k in name for k in ("unreal","dx12","ray tracing")): args.append("-dx12")`
  —— 靠**游戏名字符串**判断引擎；而指纹层已实测识别出 `engine=unity`
- `_preload_suggestions`（:121-125）：`if any(k in name for k in ("open world","gta","assassin"))`
  —— 同样靠名字猜游戏类型
- 唯一亮点：`_suggest_background_cleanup`（:86-119）**是真实采集的**，psutil 枚举内存 Top5
  且带系统关键进程白名单，实测有效

#### 4.9 `AgentFactory` 注册名与运行期路由名双轨
- `_AGENT_REGISTRY`（`agent_factory.py:197-210`）用**类型名**：`audio` / `social` / `community` / `network`
- `DEFAULT_AGENT_CONFIGS` 的 `name` 用**注册名**：`audio_expert` / `social_agent` / `community_agent` / `network_expert`
- 智能体自身 `super().__init__("audio_expert")` 用的又是**注册名**
- **实测踩坑**：`create_agent("audio_expert")` → `ValueError: Unknown agent: audio_expert`
- 当前未出事仅因 `MultiAgentSystem.agents` 的键取自 `agent.name`（注册名），
  与 `_task()` 路由名恰好一致；但 `AgentFactory` 的键（类型名）是**另一套体系**，
  任何直接用工厂名创建实例的代码都会踩空

### P2 — 健壮性与一致性

#### 4.10 `pipeline.py` 存在未导入的名字引用
- `:183-185` 用 `SearchResult` / `KnowledgeHit` / `LogEntry` 作类型注解，
  但 `:23-32` 的 `from .models import (...)` 未包含这三者
- 因 `from __future__ import annotations` 注解不求值，运行时暂不报错；
  一旦移除该 import 或被 `typing.get_type_hints()` 求值 → `NameError`

#### 4.11 `_gather_context` 的搜索降级会静默吞掉配置错误
- `pipeline.py:192-194`：`except Exception: searches = []`
- 与 `:213`、`:229-230`、`:235-237` 同款裸捕获 —— **配置错误、密钥失效、代码 bug
  全部表现为"没搜到"**，无日志无提示，排障时无从下手

#### 4.12 `compatibility` 与 `perf_security` 各采一次系统信息，结果还不一致
- `compatibility` 走 `utils/system_info.get_system_info()`（wmi 路径，GPU=Unknown）
- `fingerprint/runtime.probe_runtime()` 走 PowerShell CIM（GPU=RTX 4070）
- `perf_security._analyze_performance`（:179）又调一次 `get_system_info()`
- **同一进程内三套采集、两份矛盾结论**，且每次全量重采无缓存

#### 4.13 文档与实现漂移
- "15 个领域智能体"（`governance/__init__.py:29`、`README.md`）vs 实测 12 个
- `docs/agents_system.md:130` 示例写 `async def execute`，
  但 `:36` 的调用示例写 `result = agent.execute(task)` 无 `await`
- `docs/agents_system.md:112` 用 `await orchestrator.execute_workflow(plan, context)`，
  但同一文档 `:65` 的 CLI 示例未体现异步

---

## 五、优化方案

### 阶段一：打通采集主干（P0，收益最大）

#### 1.1 新建统一采集层 `context_builder.py`，把指纹层接进治理链路

核心思路：**采集一次、全局共享、带缓存**。指纹层能力已经足够，只是没接线。

```python
# src/gamedoctor/context_builder.py（新增）
"""统一上下文构建：把 fingerprint 三件套的采集结果灌入 GameContext，
按 game_dir 缓存，供一次请求内所有智能体共享（避免重复全量重采）。"""

from __future__ import annotations

from functools import lru_cache
from pathlib import Path

from .fingerprint.engine import detect_engine
from .fingerprint.platform import detect_platform
from .fingerprint.runtime import probe_runtime
from .models import GameContext, TechStackFingerprint


def build_fingerprint(game_name: str, game_dir: str = "") -> TechStackFingerprint:
    """采集技术栈指纹。任何单项失败都降级，不抛异常（排障链路不能因探测自身中断）。"""
    d = Path(game_dir) if game_dir else None
    engine = None
    try:
        engine = detect_engine(d) if d else None
    except Exception:
        engine = None
    try:
        runtime = probe_runtime()
    except Exception:
        from .models import RuntimeFingerprint
        runtime = RuntimeFingerprint()
    try:
        platform = detect_platform(game_name, d)
    except Exception:
        from .models import PlatformInfo
        platform = PlatformInfo(install_path=d)
    return TechStackFingerprint(game_name=game_name, engine=engine,
                                runtime=runtime, platform=platform)


def build_context(game_name: str, game_dir: str = "") -> tuple[GameContext, TechStackFingerprint]:
    """把指纹灌入 GameContext，返回 (上下文, 指纹)。"""
    fp = build_fingerprint(game_name, game_dir)
    ctx = GameContext(game_name=game_name)

    # 平台：指纹识别优先，识别不到才落 standalone
    if fp.platform:
        ctx.platform = fp.platform.platform or "standalone"
        ctx.app_id = fp.platform.app_id
        # 平台清单里的安装路径比用户手输更可信（Steam appmanifest 是权威源）
        if fp.platform.install_path:
            ctx.install_path = fp.platform.install_path
    if ctx.install_path is None and game_dir:
        ctx.install_path = Path(game_dir)

    # 目录级事实：体积与文件数（只在目录存在时算，避免无谓 IO）
    if ctx.install_path and ctx.install_path.is_dir():
        ctx.size_mb = _dir_size_mb(ctx.install_path)

    return ctx, fp


def _dir_size_mb(root: Path, cap_files: int = 20000) -> int:
    """目录体积（MB）。带文件数上限，避免超大目录拖垮首次请求。"""
    total = 0
    seen = 0
    try:
        for p in root.rglob("*"):
            seen += 1
            if seen > cap_files:
                break
            try:
                if p.is_file():
                    total += p.stat().st_size
            except OSError:
                continue
    except OSError:
        return 0
    return total // (1024 * 1024)
```

#### 1.2 改造协调器：把指纹注入上下文与任务数据

`coordinator.py:239-241` 改为：

```python
from ..context_builder import build_context

# 原：context = GameContext(game_name=game_name)
#     if game_dir: context.install_path = Path(game_dir)
context, fingerprint = build_context(game_name, game_dir)
```

并在 `_task` 的 `base` 数据里带上指纹（`planner.py:202-206` 与 `coordinator.py:402-406`、
`coordinator.py:551-555` 三处 `base`/`data` 构造点）：

```python
base = {
    "game_name": game_name,
    "game_dir": game_dir,
    "user_message": message,
    "fingerprint": fingerprint,      # 新增：引擎/运行时/平台一次到位
}
```

`orchestrator.py:229-236` 的 `_execute_task_with_context` 已经在注入 `game_context`，
指纹随 `GameContext` 自然下沉，**智能体侧无需改签名**。

#### 1.3 `GameContext` 补指纹字段

`models.py:330-342` 增补，让智能体能直接读：

```python
@dataclass
class GameContext:
    game_name: str = ""
    install_path: Path | None = None
    install_date: datetime | None = None
    last_played: datetime | None = None
    playtime_hours: float = 0.0
    platform: str = "standalone"
    app_id: str | None = None
    version: str | None = None
    size_mb: int = 0
    dlcs: list[str] = field(default_factory=list)
    # 新增：技术栈指纹（引擎 / 运行时 / GPU / 运行库），采集一次全局共享
    engine: str | None = None                 # unity / unreal / godot / renpy / electron / java
    gpu_name: str | None = None
    gpu_driver: str | None = None
    directx_version: str | None = None
    dotnet_version: str | None = None
    vc_components: list[str] = field(default_factory=list)
    os_name: str | None = None
    os_arch: str | None = None
```

`build_context` 里同步回填这些扁平字段（智能体不必懂 `TechStackFingerprint` 嵌套结构）。

### 阶段二：修死采集层的硬故障（P0）

#### 2.1 废弃 `utils/system_info.py` 的 GPU/OS/DX 采集，统一走指纹层

理由：指纹层实测全部正确，`system_info` 实测全部失真。**不要试图修 wmi 路径**——
它依赖可选包、有 `.contains()` 语法错、`Win32_DirectX` 是不存在的 WMI 类，
而 PowerShell CIM 方案已验证可用且无额外依赖。

```python
# utils/system_info.py
def get_gpu_info() -> List[GPUInfo]:
    """改用 PowerShell CIM（与 fingerprint/runtime.py 同源），去掉 wmi 硬依赖。"""
    from ..fingerprint.runtime import SystemRuntimeProbe
    name, driver = SystemRuntimeProbe()._probe_gpu_windows()
    if not name:
        return []
    return [GPUInfo(name=name, memory=0, driver_version=driver or "Unknown",
                    manufacturer=_vendor_of(name), api_support=[])]
```

同时：
- **删除 `get_system_requirements_check()`（:323-351）** —— 含 `NameError`，从未跑通，
  且全项目无调用方（已 grep 确认）。若确需该能力，重写为不含 `self` 的纯函数
- **修 `get_os_info()`（:189-223）**：改用 `platform.release()` + `DisplayVersion` 注册表值，
  不要再用遗留的 `ProductName`/`CurrentVersion`
- **删 `get_directx_info()` 的 `Win32_DirectX` 分支**，改调 `SystemRuntimeProbe()._probe_directx()`
- **显存**：CIM 的 `AdapterRAM` 是 32 位字段，>4GB 会溢出。
  建议用 `nvidia-smi --query-gpu=memory.total` 或 `Win32_VideoController` 的
  `AdapterRAM` 配合厂商工具；取不到时**明确置 `None` 而非 `0`**（见 2.2）

#### 2.2 重写 `compatibility` 判定逻辑

四处必改：

```python
# ① _is_cpu_compatible：不要 return True，做真实的代际/核心数比较
def _is_cpu_compatible(self, current_cpu: str, required_cpu: str) -> bool:
    """按代际粗判：解析出系列与代号数字，当前 >= 需求即通过。
    解析不出时返回 True（宁可漏报，不可像现在这样恒误报）。"""
    cur, req = _parse_cpu_tier(current_cpu), _parse_cpu_tier(required_cpu)
    if cur is None or req is None:
        return True
    return cur >= req

# ② _is_gpu_compatible：参数语义摆正，None 与 0 区分开
def _is_gpu_compatible(self, current_gpu: str, current_vram: int | None,
                       required_gpu: str, required_vram_mb: int | None) -> bool:
    if current_vram is None or required_vram_mb is None:
        return True          # 采不到显存 → 不下结论，而不是判不合格
    return current_vram >= required_vram_mb * 1024 * 1024

# ③ 调用处（:202）：第 4 参必须是显存需求，不是 storage
gpu_vram_mb = _read_gpu_vram_mb()      # 采不到返回 None
if not self._is_gpu_compatible(gpu, gpu_vram_mb,
                               requirements["gpu"], requirements.get("gpu_vram_mb")):
    ...

# ④ _check_os_compatibility：用真实 OS 版本，别用遗留注册表值
os_version = system_info.get("os", {}).get("release") or platform.release()
```

**更进一步**：需求表不该硬编码。建议按引擎给出默认档位（指纹层已能识别引擎），
并支持游戏目录内的需求描述文件覆盖：

```python
_REQUIREMENTS_BY_ENGINE = {
    "unity":    {"cpu": "i5-4590", "gpu": "GTX 960",  "gpu_vram_mb": 2048, "ram_mb": 8192},
    "unreal":   {"cpu": "i7-6700", "gpu": "GTX 1060", "gpu_vram_mb": 4096, "ram_mb": 12288},
    "renpy":    {"cpu": "i3-2100", "gpu": "Intel HD", "gpu_vram_mb": None, "ram_mb": 2048},
    "java":     {"cpu": "i3-2100", "gpu": "Intel HD", "gpu_vram_mb": None, "ram_mb": 4096},
}
```

`known_issues_db`（:50-61）应从硬编码示例改为读 `rag/` 下的知识文档（见阶段三）。

#### 2.3 `installation` 必须尊重传入目录

```python
def _optimize_install_path(self, game_name: str, source: str,
                           game_dir: str = "", sx=None) -> Path:
    """已装游戏：直接用传入目录，绝不重算。
    只有"全新安装、目标未定"时才走候选路径推荐。"""
    if game_dir:
        p = Path(game_dir)
        if p.exists():
            return p                      # ← 关键：已存在的目录就是安装目录
    # ...以下才是原有的候选路径推荐逻辑
```

`_find_main_executable`（:341-350）改为多策略：

```python
def _find_main_executable(self, install_path: Path) -> Path | None:
    """三策略：① 目录同名 exe（最常见的发行命名）② 固定常见名 ③ 最大 exe 兜底。"""
    if not install_path.is_dir():
        return None
    # ① 与目录同名的 exe：TestGame/TestGame.exe 是发行版主流命名
    same = install_path / f"{install_path.name}.exe"
    if same.is_file():
        return same
    # ② 常见固定名
    for exe_name in ("game.exe", "start.exe", "main.exe", "launcher.exe",
                     "play.exe", "run.exe"):
        p = install_path / exe_name
        if p.is_file():
            return p
    # ③ 顶层最大 exe 兜底（排除 unins*/setup*/redist* 等安装器）
    exes = [p for p in install_path.glob("*.exe")
            if not p.stem.lower().startswith(("unins", "setup", "install",
                                              "redist", "vcredist", "dxsetup"))]
    if not exes:
        return None
    return max(exes, key=lambda p: p.stat().st_size)
```

`_check_steam_game` / `_check_epic_game`（:372-380）**不要恒 True**。
改为调用 `detect_platform(game_name, game_dir)`，按真实平台归属回答；
探测不到就返回 `False` 并在 `pre_install_checks` 里写明"未能确认平台归属"。

#### 2.4 修知识库断链

**灌库入口（当前完全缺失）**：

```python
# src/gamedoctor/knowledge/seed.py（新增）
"""把 rag/*.md 知识文档灌入 SQLite，使零 LLM 路径也能出修复方案。"""
from pathlib import Path
from .store import SQLiteKnowledgeStore

def seed_from_rag(rag_root: Path | None = None) -> dict[str, int]:
    """扫描 rag/<game>/*.md，按 front-matter 的 signature 建索引。
    返回 {游戏名: 入库条数}。幂等：按 (signature, game) upsert。"""
    root = rag_root or Path(__file__).resolve().parents[3] / "rag"
    store = SQLiteKnowledgeStore()
    store.connect()
    counts: dict[str, int] = {}
    try:
        for game_dir in sorted(p for p in root.iterdir() if p.is_dir()):
            n = 0
            for md in sorted(game_dir.glob("*.md")):
                sig, template = _parse_md(md)      # 解析签名与修复步骤
                if sig:
                    store.upsert(sig, template, game=game_dir.name)
                    n += 1
            if n:
                counts[game_dir.name] = n
    finally:
        store.close()
    return counts
```

**修向量检索的导入路径**（`retriever.py:55`、`:171`、`:217`、`:235`）：

```python
# 错：from ...rag import rag_data     → 解析成 gamedoctor.rag（不存在）
# 对：顶层 rag/ 不是包内模块，须用 importlib 按文件路径加载
def _load_rag_module():
    import importlib.util
    root = Path(__file__).resolve().parents[3] / "rag" / "rag_data.py"
    if not root.is_file():
        return None
    spec = importlib.util.spec_from_file_location("gamedoctor_rag_data", root)
    mod = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(mod)
    return mod
```

**`config.py:48-49` 默认值指向真实目录**：

```python
"use_vector": False,
"rag_data_dir": "",   # 空则由 retriever 回退到 <项目根>/rag，而非空字符串直接用
```

并在 `search_knowledge()`（`retriever.py:188-197`）里加一次**首启自动灌库**：
库为空且 `rag/` 有文档时调 `seed_from_rag()`，让零配置也能命中。

**`langchain_core` 未安装**属环境问题，需在 `pyproject.toml` 的 `[vector]` extra 里明确，
并在 `_check_availability()` 返回 `False` 时**记一条日志说明原因**，不要静默。

### 阶段三：让占位智能体"诚实"（P1）

#### 3.1 给 `AgentResult` 增加数据来源标记

这是**成本最低、收益最高**的一项改动。当前占位数据与真实数据在结构上无法区分，
审议官和回奏只能照单全收。

```python
# agents/base_agent.py :: AgentResult
@dataclass
class AgentResult:
    success: bool
    data: Any = None
    message: str = ""
    metrics: Dict[str, Any] = None
    warnings: List[str] = None
    errors: List[str] = None
    skipped: bool = False
    # 新增：数据 provenance，让下游能区分"实测"与"占位"
    unverified: List[str] = None     # 未验证/占位的字段名列表
```

#### 3.2 占位智能体显式申报，不再伪装成结论

```python
# audio_expert.py
def _detect_devices(self) -> List[str]:
    """WASAPI 未接入前，如实返回空并申报，不伪造"默认输出设备"。"""
    return []

async def execute(self, task):
    devices = self._detect_devices()
    result = AudioResult(...)
    return AgentResult(
        success=True, data=result,
        message=f"音频检测能力未接入（设备枚举需 WASAPI）",
        unverified=["detected_devices", "format_issues", "spatial_audio_enabled"],
    )
```

`social_agent` / `community_agent` / `network_expert._detect_nat` 同款处理。
**原则：宁可返回空 + 明确说明，不要返回看起来像真的占位串。**

#### 3.3 审议官增设"占位数据"验收规则

`reviewer.py :: review_outcome`（:181-195）增补：

```python
def review_outcome(self, outcome: Dict[str, Any]) -> ReviewVerdict:
    v = ReviewVerdict()
    # ...原有 counts 检查...
    # 新增：统计申报了 unverified 的任务，提示回奏必须如实披露
    for name, r in (outcome.get("task_results") or {}).items():
        unv = getattr(r, "unverified", None)
        if unv:
            v.warnings.append(
                f"「{name}」有 {len(unv)} 项数据未经实测（{', '.join(unv)}），"
                f"交付时须标注为待确认，不得作为结论陈述")
    return v
```

#### 3.4 有真实数据源可接的，优先接上

按性价比排序：

| 智能体 | 可接的真实数据源 | 成本 |
|---|---|---|
| `launch_optimizer` | **已接指纹层即可**：用 `ctx.engine` 替代游戏名猜引擎；`ctx.gpu_name` 判断是否给 `-dx12` | 极低（改 3 行） |
| `perf_security` | 显存改从 CIM/nvidia-smi 取，取不到置 `None`（避免 `0 < 4GB` 恒判瓶颈） | 低 |
| `audio_expert` | Windows：`Get-CimInstance Win32_SoundDevice`（与现有 CIM 方案同源，无新依赖） | 低 |
| `network_expert` | NAT：STUN 需新依赖，可后置；但**端口表应按游戏/引擎配置化**，别硬编码 | 中 |
| `game_content` | `version`：Steam 可读 `appmanifest_*.acf` 的 `Version`/`BuildID`（平台层已能定位清单） | 中 |
| `social_agent` | 无本地数据源，**建议保持占位 + 申报**，不要伪造 | — |
| `community_agent` | 需外部 API，**建议保持占位 + 申报**；或复用 `web_search` 能力而非另造 | — |

### 阶段四：架构一致性与可维护性（P1/P2）

#### 4.1 统一 `AgentFactory` 命名双轨

`_AGENT_REGISTRY`（`agent_factory.py:197-210`）的键改为与运行期路由名一致：

```python
# 键统一为注册名（与 DEFAULT_AGENT_CONFIGS["name"]、BaseAgent.name 三者对齐）
_AGENT_REGISTRY = [
    ("compatibility",    "..agents.compatibility.compatibility_agent", "CompatibilityAgent"),
    ("installation",     "..agents.installation.installation_agent",   "InstallationAgent"),
    ("game_content",     "..agents.content.game_content_agent",        "GameContentAgent"),
    ("launch_optimizer", "..agents.optimization.launch_optimizer_agent","LaunchOptimizerAgent"),
    ("community_agent",  "..agents.community.community_agent",         "CommunityAgent"),
    ("social_agent",     "..agents.social.social_agent",               "SocialAgent"),
    ("perf_security",    "..agents.health.perf_security_agent",        "PerfSecurityAgent"),
    ("audio_expert",     "..agents.audio.audio_expert_agent",          "AudioExpertAgent"),
    ("network_expert",   "..agents.network.network_expert_agent",      "NetworkExpertAgent"),
    ("script_editor",    "..agents.scripts.script_editor_agent",       "ScriptEditorAgent"),
    ("web_search",       "..agents.web.web_search_agent",              "WebSearchAgent"),
    ("log_analyzer",     "..agents.logs.log_analyzer_agent",           "LogAnalyzerAgent"),
]
```

同时 `DEFAULT_AGENT_CONFIGS` 里的 `"type"` 字段随之对齐，消除两套名字。
**加一条启动期自检**：注册名、工厂键、`BaseAgent.name` 三者不一致就 `raise`，
避免以后再漂移。

#### 4.2 合并链路 A 与链路 B

现状是两套编排各自为政。建议：**保留 `GovernanceCoordinator` 作为唯一入口**，
把 `DiagnosisPipeline` 降级为"排障领域的确定性工作流模板"，纳入
`WORKFLOW_TEMPLATES`（`orchestrator.py:623-628`）：

```python
WORKFLOW_TEMPLATES = {
    "diagnostic":   GameAgentOrchestrator.create_diagnostic_workflow,
    "installation": GameAgentOrchestrator.create_installation_workflow,
    "optimization": GameAgentOrchestrator.create_optimization_workflow,
    "maintenance":  GameAgentOrchestrator.create_maintenance_workflow,
    # 新增：把 pipeline 的六层链路包成一个模板，共享同一套上下文与审议
    "deep_diagnose": create_deep_diagnose_workflow,
}
```

这样 `pipeline.py` 的知识检索 + 修复原语能力，也能享有审议官的安全门与状态机审计。

#### 4.3 系统信息单次采集 + 进程内缓存

```python
# context_builder.py
@lru_cache(maxsize=4)
def _cached_runtime() -> RuntimeFingerprint:
    """运行时探测结果按进程缓存：一次请求内多个智能体共享，
    避免 compatibility / perf_security 各自全量重采（实测两者结论还不一致）。"""
    return probe_runtime()
```

`probe_runtime()` 会起 PowerShell 子进程（`runtime.py:215`），实测有明显耗时，
缓存后一次请求只探一次。

#### 4.4 裸 `except` 补日志

`pipeline.py:192-194`、`:213`、`:229-230`、`:235-237`、
`retriever.py:131-132`、`:145-148`、`:183-185` 全部改为：

```python
except Exception:
    logging.getLogger("gamedoctor.pipeline").warning(
        "联网检索降级为空：查询=%s", queries, exc_info=True)
    searches = []
```

**降级可以，静默不行。** 当前配置错误、密钥失效、代码 bug 全表现为"没搜到"，
本次排查知识库断链时正是因为无日志才需要逐层实测定位。

#### 4.5 修 `pipeline.py` 未导入的名字

`:23-32` 的导入列表补上 `KnowledgeHit`、`LogEntry`、`SearchResult`。

#### 4.6 同步文档

- `governance/__init__.py:29` 与 `README.md`：15 → **12** 个领域智能体
- `docs/agents_system.md`：补 `await`（:36、:65）、补真实注册名表、
  补"哪些智能体不需要 LLM"一节

---

## 六、落地顺序建议

| 批次 | 内容 | 依赖 | 验证方式 |
|---|---|---|---|
| **第 1 批** | 1.1 `context_builder` + 1.2 协调器注入 + 1.3 `GameContext` 扩字段 | 无 | 跑仿真目录，断言 `ctx.gpu_name == "NVIDIA GeForce RTX 4070 Laptop GPU"`、`ctx.engine == "unity"`、`ctx.platform` 非默认值 |
| **第 2 批** | 2.1 废弃 `system_info` 失真采集 + 删 `get_system_requirements_check` | 第 1 批 | `get_gpu_info()` 返回真实型号；`get_system_requirements_check` 不再存在于代码库 |
| **第 3 批** | 2.2 重写兼容性判定（4 处） | 第 1、2 批 | 本机（RTX 4070 / Win11）`overall_score` 应为高分，`critical_issues` 应为空；构造 Win7 环境应真实报出 OS 不合格 |
| **第 4 批** | 2.3 `installation` 尊重 `game_dir` + 多策略找 exe + 平台探测替代恒 True | 第 1 批 | 传仿真目录 → `install_path` 等于传入值；`verification.success=True` 且找到 `TestGame.exe` |
| **第 5 批** | 2.4 知识库灌库 + 向量导入路径修正 + 首启自动灌库 | 无 | `search_knowledge("missing dll d3dx9_43.dll")` 返回非空；`get_available_games()` 返回 `['Grand_Theft_Auto_V','minecraft']` |
| **第 6 批** | 3.1 `AgentResult.unverified` + 3.2 占位申报 + 3.3 审议官新规则 | 无 | `audio`/`social`/`community` 的 `unverified` 非空；`review_outcome` 产出对应 warning |
| **第 7 批** | 3.4 可接数据源的接入（按性价比表） | 第 1 批 | 各智能体返回实测数据 |
| **第 8 批** | 4.1 命名统一 + 启动自检 → 4.2 链路合并 → 4.3 缓存 → 4.4 日志 → 4.5/4.6 修补与文档 | 前序 | 全量单测通过；`AgentFactory.create_agent("audio_expert")` 不再抛 `Unknown agent` |

### 回归测试补充建议

现有 `tests/` 覆盖了治理层（`test_governance.py`）、模型、策略、备份、文本编辑，
但**缺采集层测试**。建议新增：

- `tests/test_context_builder.py`：造仿真目录，断言引擎/平台/GPU/运行库全部灌入 `GameContext`
- `tests/test_system_info.py`：断言 `get_gpu_info()` 不再返回 `Unknown GPU`（有 CIM 时）；
  断言 `get_system_requirements_check` 已移除或可正常调用
- `tests/test_compatibility_truth.py`：**用真实采集数据做端到端断言**——
  这类测试正是本次发现"恒误报"的缺失环节。当前 `test_models.py` 只测数据结构，
  测不出判定逻辑失效
- `tests/test_installation_dir.py`：断言传入已存在目录时 `install_path` 原样返回
- `tests/test_knowledge_seed.py`：灌库后断言已知签名命中

---

## 七、一句话总结

**制度层是好的，采集层是断的。**

三省六部的状态机、权限矩阵、纯规则审议官设计与实现都扎实，实测零 LLM 也能跑通
意图分拣 → 模板规划 → 安全审议 → 权限派发 → 验收全链路。

但项目里**已经有一套能正确采集 RTX 4070 / DX12 Ultimate / .NET 4.8 / 10 个 VC++ 组件 /
unity 引擎的指纹层，治理链路却一次都没调用它**；智能体只能退回去用一套
含 `NameError` 死代码、`.contains()` 语法错、不存在的 WMI 类的残缺采集层，
于是本机被误判为"GPU 不合格 + 系统版本过低"。
再加上 33 篇知识文档因灌库缺失与导入路径错位而零命中，
最终形成"**采集失真 → 知识库空 → 只能靠 LLM → LLM 一挂就零动作**"的脆弱链条。

优先做第 1 批（接线）与第 5 批（灌库），
这两项都不需要新写复杂逻辑，只是把已有能力接上，
但能立刻把"无大模型自动采集"从当前的 4 个智能体部分可用，
提升到全链路可用。
