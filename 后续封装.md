## 打包分发（核心是解决"conda 手起后端"，分三档）
现在 launcher.py 的策略是"找不到 exe 就现场`dotnet build` "——这要求用户机装 .NET SDK，开发者能忍、玩家不能忍。建议：

- 第一档（开发者，今天就能用） ：`pipx install .` 或`pip install -e '.[server]'` ，`gameAgent` 一条命令起前后端。零额外工作，只需验证 pipx 路径无误。
- 第二档（测试用户，推荐目标） ：一体化绿色目录
  1. `dotnet publish -c Release -r win-x64 --self-contained` 出 WPF（免装 .NET 运行时，约 150MB）
  2. PyInstaller onedir （不用 onefile，启动快且杀软误报少）打后端，入口`launcher.py` ，`--noconsole`
  3. 把 WPF 发布产物作为 package-data 放进 Python 包，launcher 改为 优先启动内嵌 exe、找不到才回退现场构建 （约 20 行改动）
  4. 一个`scripts/build_release.ps1` 串起来，产物 zip + SHA256
- 第三档（正式发布，以后再说） ：Inno Setup 做安装器（开始菜单快捷方式、卸载）、代码签名（不签名 SmartScreen 会拦，要有心理预期）
第二档做完，Task 7 那种环境问题彻底消失——用户解压即用，不依赖任何 Python/conda。工作量约 1 天，主要花在 PyInstaller hiddenimports 调参和真机验证。